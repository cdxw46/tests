## Cursor Cloud specific instructions

This repository is currently a bare/placeholder repo with no application code, build system, or dependencies. The only content file is `aa` (a trivial placeholder).

- **No services to start.** There is no application, backend, frontend, or database.
- **No dependencies to install.** There are no package manifests (`package.json`, `requirements.txt`, `pyproject.toml`, etc.).
- **No lint, test, or build commands.** No tooling is configured.

When application code is added in the future, update this section with startup caveats, service descriptions, and non-obvious development notes.
